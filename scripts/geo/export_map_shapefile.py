#!/usr/bin/env python
"""Export all tile masks in a folder to per-tile shapefiles + one combined map shapefile."""

from tree_seg.cli import export_map_shapefile_main

if __name__ == "__main__":
    export_map_shapefile_main()
